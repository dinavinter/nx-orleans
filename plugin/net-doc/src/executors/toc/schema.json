{
  "properties": {
    "input": {
      "type": "string",
      "description": "Folder containing the documents.",
      "default": "{projectRoot}"
    },
    "output": {
      "type": "string",
      "description": "Folder to write the resulting toc.yml in.",
      "default": "{projectRoot}"
    },
    "verbose": {
      "type": "boolean",
      "description": "Show verbose messages.",
      "default": false
    },
    "sequence": {
      "type": "boolean",
      "description": "Use the .order files for TOC sequence. Format are raws of: filename-without-extension",
      "default": false
    },
    "override": {
      "type": "boolean",
      "description": "Use the .override files for TOC file name override. Format are raws of: filename-without-extension;Title you want",
      "default": false
    },
    "index": {
      "type": "boolean",
      "description": "Auto-generate a file index in each folder.",
      "default": false
    },
    "ignore": {
      "type": "boolean",
      "description": "Use the .ignore files for TOC directory ignore. Format are raws of directory names: directory-to-ignore",
      "default": false
    },
    "multitoc": {
      "type": "number",
      "description": "Generate multiple toc files for child folders down to a certain child depth, default is 0 (root only generation).",
      "default": 0
    },
    "skipInstall": {
      "type": "boolean",
      "description": "Whether to skip installing dependencies.",
      "default": false
    }
  }
}


